# SDAH Update 3-24-26 6:37pm

**Date:** 2026-03-24, 6:37pm (local)  
**Site:** San Diego Amazing Homes (SDAH)  
**Project path:** `Websites/sandiegoamazinghomes/`

---

## Summary

This update improves neighborhood imagery (curated area photos + larger list cards), makes the homepage Instagram strip reflect newer posts (folder order + optional Meta API), adds a blog index hero tied to a sunset asset in `sdah photos to use`, and aligns the matches page with the same hero resolution as the neighborhoods index.

---

## 1) Neighborhood hero & carousel (`/neighborhoods`, detail pages)

- **`lib/neighborhood-images.js`**
  - Resolution order: per-slug folder under `public/images/neighborhoods/{slug}/` (now includes `.png`), then curated files under `public/images/sdah photos to use/Areas/` or `areas/`, then region `_fallbacks`, then global placeholder.
  - Slug → filename map for client-provided area PNGs; URLs encode spaces in path segments.
- **`app/matches/page.js`**
  - Match cards use `getNeighborhoodHeroPath(slug, region)` so they stay in sync with resolved heroes (not raw `_index.json` placeholders).

Doc: `docs/Neighborhood-Areas-Photos-Sdah-Folder-2026-03-24.md`

---

## 2) Neighborhood list layout (`/neighborhoods`)

- **`components/neighborhoods/NeighborhoodList.js`**
  - Horizontal rows scaled up: thumbnails **144×96** (default) and **176×112** (`sm+`), increased padding, larger name/region/intro/score chips and “View →”.

Doc: `docs/Neighborhood-List-Row-Size-2026-03-25.md`

---

## 3) Homepage Instagram module (`/`)

- **`lib/instagram-graph.js`** (new)
  - Optional **`INSTAGRAM_ACCESS_TOKEN`** (+ optional **`INSTAGRAM_USER_ID`**) pulls latest media from Instagram Graph endpoints; homepage **`revalidate = 3600`**.
- **`app/page.js`**
  - Folder-based Instaloader images: **newest first**, max **24**, excludes `profile_pic` filenames; tries API when token is set.
- **`components/ui/ImageCarousel.js`**
  - Per-slide `href` / `permalink`, `unoptimized` for external `https://` image URLs.
- **`scripts/sync-instagram.js`**
  - Adds **`--fast-update`** for incremental Instaloader runs (Instagram may still return **429** if rate-limited).

Docs: `docs/Instagram-Module-Live-Feed-2026-03-24.md` · `.env.example` updated · `README.md` Instagram section updated

---

## 4) Blog index hero (`/blog`)

- **`lib/blog-hero-image.js`** (new)
  - Resolves an image under `public/images/sdah photos to use/` (recursive) whose filename contains **`sunset`**; **most recently modified** wins if several match.
- **`app/blog/page.js`**
  - Full-width (within site column) **~16:9** hero, capped height, subtle bottom gradient, line: **“San Diego Real Estate Insights”**; gradient fallback if no matching file.
- **`public/images/sdah photos to use/README.md`**
  - Note on naming the sunset file for the blog hero.

Doc: `docs/Blog-Hero-Sunset-2026-03-25.md`

---

## 5) Project documentation & state

- Updated: `docs/Agent-Handoff.md`
- Updated: `Websites/docs/Website-State.json` (routes, `changeLog`, `lastUpdated` as applicable)

---

## Dev server

- SDAH runs at **`http://localhost:3001`** via **`npm run dev`** (`scripts/dev-3001.js`).

---

## PDF

- **Not generated by default.** If a PDF is needed, run from project root:  
  `node scripts/generate-update-report-pdf.js sdah-update-3-24-26-6-37pm`
