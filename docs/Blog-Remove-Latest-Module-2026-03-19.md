# Blog index: remove “Latest” module — 2026-03-19

## Goal

Remove the featured “Latest” post block from the blog index so the page is simpler and only shows the full list.

## Change

- `app/blog/page.js`: removed the `latest` featured module and its related `Calendar` icon import.

