# Market data adapters (Phase 1 stubs)

- **redfin.js** — Redfin market data (e.g. by ZIP or region). TODO: Add REDFIN_* or use public/scraped metrics; respect ToS.
- **zillow.js** — Zillow public metrics. TODO: Add ZILLOW_* if API available; else use documented public data only.
- **rentcast.js** — RentCast market stats. TODO: Same API key as listings; neighborhood-level metrics.

No live API keys or production calls in Phase 1.
