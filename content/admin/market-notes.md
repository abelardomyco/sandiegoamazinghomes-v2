# Market page — internal notes

Short reminders for the live `/market` page (edit markdown in repo; shown on `/admin/dashboard` only).

- Keep copy aligned with `app/market/page.js` and `lib/market-data.js`.
- If RentCast or Supabase stats go live, note the data refresh cadence here.

_Last updated: pre-launch pass._
